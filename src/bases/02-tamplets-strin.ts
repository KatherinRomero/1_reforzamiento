const firstName='fernando'
const lastName="O'Neal"

const product1=15
const product2=20

const fullName=`
El nombre completo es ${firstName} ${lastName} y su total es de ${product1+product2}$
`  
//${} se usa para insertar variables o expresiones dentro de un string
console.log(fullName)